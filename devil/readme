$ Requires wx, twisted and python2.7

Commands 
$ ./file.py --help to see lists of available commands 

$ To pull from a server you should have a server running on the remote 
$ ./file.py --pull [ip]:[Port][Fullpath of directory at remote]

$ To push 
$ ./file.py --push [your]:[your port][remote ip]:[remote post][full path of remote directory]

Rest of the commnads are described in the document. 
